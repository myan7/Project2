package my.spring.model;

import javax.persistence.Entity;
import javax.persistence.Id;

@Entity
public class OrderHistroy {

	@Id
	long id;
	/*
	 * write a dao, service, and controller, make change to the jsp
	 * userid, itemid, haspaid
	 */
}
